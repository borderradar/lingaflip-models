# Git LFS — track all ONNX model files as large binary objects
voices/**/*.onnx filter=lfs diff=lfs merge=lfs -text

# Normal text files — tracked by git as usual
*.md text eol=lf
*.json text eol=lf
*.py text eol=lf
*.txt text eol=lf
